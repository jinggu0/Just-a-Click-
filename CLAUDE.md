# Claude 프로젝트 지침

이 프로젝트의 공통 작업 지침은 아래 문서를 따른다.

@AGENTS.md

자동 import를 지원하지 않는 환경에서는 작업 전에 `AGENTS.md`를 직접 읽고 동일하게 적용한다.

## 시작 순서

1. `AGENTS.md`를 읽는다.
2. 작업과 관련된 `docs/PRD.md` 요구사항과 `docs/ROADMAP.md` 단계·완료 조건을 확인한다.
3. 현재 코드와 Git 변경 상태를 확인한 뒤 필요한 작업만 진행한다.

최종 응답은 공통 지침대로 **작업 / 결과 / 다음** 세 항목을 중심으로 한국어로 간결하게 작성한다. 검증하지 않은 내용은 미검증으로 표시한다.

공통 정책을 바꿀 때는 `AGENTS.md`를 수정한다. 이 파일에는 Claude에만 필요한 시작·참조 방법을 유지하여 두 문서의 정책이 어긋나지 않게 한다.
